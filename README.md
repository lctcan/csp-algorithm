# csp-algorithm

深度优先搜索算法

思想：一条路走到黑，走到尽头再回溯，回到上一步继续探索，直到得到解或者无解为止   
实现：用数据结构 栈  /  用函数调用实现   
搜索顺序：纵向深入

都有这些基本东西
int net[MAX][MAX] // 二维数组的地图
int dx[4] = {-1,1,0,0}  // 方向数组， 遍历时，需要有当前坐标和下一坐标
int dy[4] = {0,0,1,-1}.
int visited[MAX]  // 标记已访问
